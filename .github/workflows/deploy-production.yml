# =====================================================
# 本番デプロイ ワークフロー (Reflex)
# mainプッシュ時に実行（CI通過後）
# =====================================================
#
# デプロイ先オプション:
# 1. Reflex Cloud (推奨): reflex deploy
# 2. Docker + Cloud Run / Railway / Fly.io
# 3. セルフホスト (VPS)
#
# 現在: Reflex Cloud を使用

name: 本番デプロイ

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production-deploy
  cancel-in-progress: false

env:
  PYTHON_VERSION: '3.11'

permissions:
  contents: read
  statuses: write

jobs:
  deploy:
    name: "🚀 Reflex Cloudへデプロイ"
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Reflex デプロイ
        id: deploy
        run: |
          # Reflex Cloud へデプロイ
          # 注意: REFLEX_TOKEN シークレットが必要
          if [ -n "${{ secrets.REFLEX_TOKEN }}" ]; then
            reflex deploy --no-interactive
            echo "url=https://management-dashboard.reflex.run" >> $GITHUB_OUTPUT
          else
            echo "::warning::REFLEX_TOKEN が設定されていません。デプロイをスキップします。"
            echo "url=https://localhost:3000" >> $GITHUB_OUTPUT
          fi
        env:
          REFLEX_TOKEN: ${{ secrets.REFLEX_TOKEN }}
        continue-on-error: true  # 初期段階では Reflex Cloud 設定がない場合がある

      - name: デプロイURL通知
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.sha.substring(0, 7);
            const url = '${{ steps.deploy.outputs.url }}';

            // デプロイステータス作成
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: 'success',
              target_url: url,
              description: '本番デプロイ成功',
              context: 'Reflex本番'
            });

            console.log(`Deployed commit ${sha} to ${url}`);

      - name: 📊 Deploy Summary
        if: always()
        run: |
          echo "## 🚀 デプロイ結果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 項目 | 値 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| コミット | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 環境 | production |" >> $GITHUB_STEP_SUMMARY
          echo "| URL | ${{ steps.deploy.outputs.url }} |" >> $GITHUB_STEP_SUMMARY
