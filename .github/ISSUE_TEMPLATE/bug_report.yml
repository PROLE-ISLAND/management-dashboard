name: "[バグ] Bug Report"
description: バグ報告・不具合修正
title: "[バグ]: "
labels: ["bug", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        ## バグ報告テンプレート

        📚 参考: [DoD基準](https://github.com/PROLE-ISLAND/.github/blob/main/DoD_STANDARDS.md)

  - type: dropdown
    id: priority
    attributes:
      label: 優先度
      description: バグの緊急度を選択
      options:
        - P0: Critical (本番障害)
        - P1: High (機能停止)
        - P2: Medium (回避策あり)
        - P3: Low (軽微な不具合)
      default: 2
    validations:
      required: true

  - type: dropdown
    id: dod_level
    attributes:
      label: DoD Level（品質基準）
      options:
        - Bronze (80%カバレッジ) - 緊急修正
        - Silver (85%カバレッジ) - 推奨
        - Gold (95%カバレッジ) - 本番品質
      default: 1
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ## 🔴 問題定義

  - type: textarea
    id: bug_description
    attributes:
      label: バグの説明
      description: 何が起きているか具体的に記述
      placeholder: |
        例: ダッシュボードで売上データが表示されない
    validations:
      required: true

  - type: textarea
    id: steps_to_reproduce
    attributes:
      label: 再現手順
      description: バグを再現する手順
      value: |
        1.
        2.
        3.
    validations:
      required: true

  - type: textarea
    id: expected_behavior
    attributes:
      label: 期待される動作
      description: 本来どうあるべきか
    validations:
      required: true

  - type: textarea
    id: actual_behavior
    attributes:
      label: 実際の動作
      description: 現在何が起きているか
    validations:
      required: true

  - type: textarea
    id: affected_users
    attributes:
      label: 影響を受けるユーザー
      description: 誰が・どの程度影響を受けているか
      placeholder: |
        例:
        - 全ユーザー: ダッシュボードにアクセスできない
        - 影響度: 高（業務停止レベル）

  - type: markdown
    attributes:
      value: |
        ---
        ## 🔧 技術情報

  - type: textarea
    id: affected_files
    attributes:
      label: 影響するファイル
      description: 関連するファイルパス
      placeholder: |
        - src/components/Dashboard.tsx
        - src/lib/api/sales.ts

  - type: textarea
    id: error_logs
    attributes:
      label: エラーログ・スタックトレース
      description: コンソールエラー、サーバーログなど
      render: shell

  - type: checkboxes
    id: impact_scope
    attributes:
      label: 影響範囲
      options:
        - label: "フロントエンド"
        - label: "バックエンド"
        - label: "データベース"
        - label: "インフラ"

  - type: markdown
    attributes:
      value: |
        ---
        ## ✅ 受け入れ条件

  - type: textarea
    id: acceptance_criteria
    attributes:
      label: 受け入れ条件
      description: 修正完了とみなす条件
      value: |
        - [ ] 再現手順で問題が発生しなくなる
        - [ ] 回帰テストが追加されている
    validations:
      required: true

  - type: checkboxes
    id: checklist
    attributes:
      label: 事前確認
      options:
        - label: 重複Issueがないことを確認しました
          required: true
