name: "[機能] Feature Request"
description: 機能要望・新機能追加
title: "[機能]: "
labels: ["feature", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        ## 機能要望テンプレート

        📚 参考:
        - [DoD基準](https://github.com/PROLE-ISLAND/.github/blob/main/DoD_STANDARDS.md)
        - [調査スキル活用ガイド](https://github.com/PROLE-ISLAND/.github/wiki/調査スキル活用ガイド)

        ### ⚠️ 調査ファースト原則
        **Issue作成前に既存システムの調査を行ってください。**
        `/investigate` コマンドで調査レポートを作成し、このIssueにリンクしてください。

  - type: dropdown
    id: priority
    attributes:
      label: 優先度
      description: 機能の緊急度を選択
      options:
        - P0: Critical (本番障害相当)
        - P1: High (今週対応)
        - P2: Medium (今スプリント)
        - P3: Low (バックログ)
      default: 2
    validations:
      required: true

  - type: dropdown
    id: dod_level
    attributes:
      label: DoD Level（品質基準）
      description: |
        この機能に求める品質レベル。
        詳細: https://github.com/PROLE-ISLAND/.github/blob/main/DoD_STANDARDS.md
      options:
        - Bronze (80%カバレッジ) - プロトタイプ
        - Silver (85%カバレッジ) - 推奨
        - Gold (95%カバレッジ) - 本番品質
      default: 1
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ## 📋 事前調査（Investigation）

  - type: input
    id: investigation_report
    attributes:
      label: 調査レポートリンク
      description: |
        `/investigate` で作成した調査レポートへのリンク。
        未調査の場合は「未調査」と記入。
      placeholder: "https://github.com/... または「未調査」"
    validations:
      required: true

  - type: input
    id: existing_system_name
    attributes:
      label: 関連する既存システム名
      description: |
        この機能が拡張・変更する既存システムの名前。
        新規システムの場合は「新規」と記入。
      placeholder: "例: management-dashboard, hybrand"

  - type: markdown
    attributes:
      value: |
        ---
        ## 🔴 問題定義（なぜ必要か）

  - type: textarea
    id: current_problem
    attributes:
      label: 現状の課題
      description: 現在どんな問題・不便が発生しているか具体的に記述
      placeholder: |
        例: 複数のShopifyストアの売上確認に個別ログインが必要で、
        全体像の把握に毎日15-30分かかっている
    validations:
      required: true

  - type: textarea
    id: affected_people
    attributes:
      label: 影響を受ける人
      description: 誰がこの問題で困っているか - 役割を明記
      placeholder: |
        例:
        - マネージャー: 日次の売上確認に時間がかかる
        - 経営層: 月次レポート作成時に全体把握が困難
    validations:
      required: true

  - type: textarea
    id: impact_level
    attributes:
      label: 問題の影響度
      description: この問題による損失・非効率を定量的に記述
      placeholder: |
        例:
        - 毎日15-30分の作業時間ロス
        - 手動集計によるヒューマンエラーリスク
        - リアルタイム異常検知ができない

  - type: markdown
    attributes:
      value: |
        ---
        ## 🎯 ゴール候補（/req で1つ選択）

  - type: textarea
    id: goal_option_a
    attributes:
      label: 案A
      description: 最も推奨するアプローチ
      value: |
        **アプローチ名**:
        **概要**:
        **メリット**:
        **デメリット**:
        **想定工数**: 小/中/大
    validations:
      required: true

  - type: textarea
    id: goal_option_b
    attributes:
      label: 案B
      description: 代替アプローチ
      value: |
        **アプローチ名**:
        **概要**:
        **メリット**:
        **デメリット**:
        **想定工数**: 小/中/大
    validations:
      required: true

  - type: textarea
    id: goal_option_c
    attributes:
      label: 案C（任意）
      description: その他のアプローチ（任意）
      value: |
        **アプローチ名**:
        **概要**:
        **メリット**:
        **デメリット**:
        **想定工数**: 小/中/大

  - type: textarea
    id: recommended_goal
    attributes:
      label: 暫定推奨
      description: どの案を推奨するか、その理由
      placeholder: "案A - 理由: ..."
    validations:
      required: true

  - type: textarea
    id: success_metrics
    attributes:
      label: 成功指標（仮 - /req で詳細化）
      description: 「解決した」と言えるための測定可能な基準
      value: |
        - [ ]
        - [ ]
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ## 📝 機能定義

  - type: textarea
    id: description
    attributes:
      label: 機能概要
      description: 解決策の概要（詳細設計は /req で行う）
      placeholder: どのような機能を追加したいですか？
    validations:
      required: true

  - type: textarea
    id: usecase_definition
    attributes:
      label: ユースケース定義（Role × Outcome）
      description: この機能のユースケースを「誰が」「何を達成するか」で定義
      value: |
        | UC-ID | Role | Outcome | Channel |
        |-------|------|---------|---------|
        | UC-XXX-YYY-ZZZ-WEB | {役割} | {達成したいこと} | WEB/API |
    validations:
      required: true

  - type: dropdown
    id: gold_candidate
    attributes:
      label: Gold E2E 候補か？
      description: この機能は Gold E2E テストの対象候補か？
      options:
        - "いいえ（軽微な機能・内部改善）"
        - "はい（主要フロー・ビジネス影響大）"
        - "要検討（Triageで判断）"
      default: 0
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ## 🎨 UI開発（該当する場合）

  - type: input
    id: v0_link
    attributes:
      label: v0 Link
      description: |
        v0で生成したUIコンポーネントのリンク。
        バックエンドのみの機能は「N/A」と記入。
      placeholder: "https://v0.dev/chat/... または N/A"

  - type: checkboxes
    id: ui_variants
    attributes:
      label: UIバリアント
      description: 実装するUIバリアントをチェック
      options:
        - label: "Default（正常表示）"
        - label: "Loading（読み込み中）"
        - label: "Empty（データなし）"
        - label: "Error（エラー表示）"
        - label: "バックエンドのみ（UI変更なし）"

  - type: markdown
    attributes:
      value: |
        ---
        ## 📊 影響範囲

  - type: checkboxes
    id: impact_scope
    attributes:
      label: 変更対象
      description: この変更が影響する範囲をチェック
      options:
        - label: "フロントエンド（コンポーネント）"
        - label: "バックエンド（API）"
        - label: "データベース（スキーマ/マイグレーション）"
        - label: "インフラ（CI/CD、設定）"
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ## ✅ 受け入れ条件

  - type: textarea
    id: acceptance_criteria
    attributes:
      label: 受け入れ条件
      description: 完了とみなす条件をチェックリストで記入
      value: |
        - [ ]
        - [ ]
        - [ ] テストが追加されている
    validations:
      required: true

  - type: checkboxes
    id: checklist
    attributes:
      label: 事前確認
      options:
        - label: 重複Issueがないことを確認しました
          required: true
        - label: 調査レポートを作成した（または調査不要の理由がある）
