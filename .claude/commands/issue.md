# /issue - Issue作成ウィザード

このコマンドは、組織ルールに準拠したIssueを作成するためのウィザードです。

## 実行フロー

### Step 1: 調査ファースト確認

まず、調査レポートの有無を確認してください：

1. **調査レポートがある場合**: リンクを教えてください
2. **調査レポートがない場合**: `/investigate` を先に実行するよう促してください

> 📚 参考: [調査スキル活用ガイド](https://github.com/PROLE-ISLAND/.github/wiki/調査スキル活用ガイド)

### Step 2: 必須情報の収集

以下の情報をユーザーに確認してください：

| 項目 | 選択肢 | 説明 |
|------|--------|------|
| **Issue種別** | Feature / Bug / Task | Issue テンプレート選択 |
| **優先度** | P0 / P1 / P2 / P3 | P0=緊急, P1=高, P2=中, P3=低 |
| **DoD Level** | Bronze / Silver / Gold | 必要な品質レベル |

### Step 3: Feature Request の場合

以下のセクションを記入サポートしてください：

```markdown
## 📋 事前調査
- **調査レポートリンク**: {link}
- **既存システム名**: {name}

## 📝 機能定義

### 優先度
{P0/P1/P2/P3}

### DoD Level
{Bronze/Silver/Gold}

### 機能説明
{description}

### ユースケース定義（Role × Outcome × Channel）
| UC-ID | Role | Outcome | Channel |
|-------|------|---------|---------|
| UC-{DOMAIN}-{ROLE}-{OUTCOME}-{CHANNEL} | | | WEB/API/EMAIL |

### Gold E2E 候補か？
- [ ] 5つのレンズ評価済み

## 🎨 UI開発（該当する場合）
- **v0 Link**:
- **バリアント**: Default / Loading / Empty / Error

## 📊 影響範囲
- [ ] フロントエンド
- [ ] バックエンド
- [ ] データベース
- [ ] インフラ

## ✅ 受け入れ条件
- [ ]
- [ ]
- [ ]
```

### Step 4: 複雑度チェック

以下のドメインマーカーが3つ以上含まれる場合、Sub-Issue分割を提案してください：

- DB変更 / database
- API変更 / backend
- UI変更 / frontend
- E2Eテスト
- マイグレーション

**分割が必要な場合のテンプレート：**
```bash
# 親Issue
gh issue create --title "feat: {機能名}" --body-file /tmp/claude-cmd-issue-parent.md

# Sub-Issues
gh issue create --title "feat(db): {機能名} - DBスキーマ" --body-file /tmp/claude-cmd-issue-db.md
gh issue create --title "feat(api): {機能名} - API" --body-file /tmp/claude-cmd-issue-api.md
gh issue create --title "feat(ui): {機能名} - UI" --body-file /tmp/claude-cmd-issue-ui.md
```

### Step 5: Issue作成

1. ボディ内容を `/tmp/claude-cmd-issue.md` に保存
2. 以下のコマンドで作成：

```bash
gh issue create --title "{type}: {title}" --body-file /tmp/claude-cmd-issue.md --label "{labels}"
```

**重要**:
- 必ず `--body-file` を使用（`--body` は禁止）
- ファイルパスは `/tmp/claude-cmd-` で始める（Hook識別用）

## 観点チェックリスト

作成前に以下を確認：

- [ ] 調査レポートがリンクされている
- [ ] 優先度が選択されている
- [ ] DoD Levelが選択されている
- [ ] ユースケースがRole × Outcomeで定義されている（Feature）
- [ ] 受け入れ条件が記載されている
- [ ] 複雑度チェック済み（3ドメイン以上なら分割提案）
